# MCP Task Management Server Dependencies

# Core MCP server framework
# Note: Replace with actual MCP Python library once identified
# For now, placeholder for MCP server implementation
mcp-server>=0.1.0

# Built-in modules (no installation needed, listed for documentation):
# - sqlite3 (database operations)
# - json (JSON handling)  
# - base64 (project ID encoding)
# - datetime (timestamps)
# - logging (debug output)

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
